import styled from "styled-components";

export const VersionEntryStyle = styled.div`
  padding: 20px;
  margin-top: 20px;

  .versionContainer {
    display: flex;
    justify-content: center;
  }

  .noteType {
    width: 12rem;
  }

  .noteSection {
    display: flex;
  }

  .dateEntry {
    width: 12rem;
  }

  .noteData {
    width: 500px;
  }

  .addButton{
    position: absolute;
    bottom: 16px;
    right: 16px;
  }
`;

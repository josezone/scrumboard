import styled from "styled-components";

export const SPTableWrapper = styled.div`
  padding-top: 3rem;
  margin: 0 auto;

  .project {
    color: #1976d2;
  }

  h1 {
    font-family: "Roboto", "Helvetica", "Arial", sans-serif;
    font-weight: 500;
    font-size: 1.1rem;
    line-height: 1.25;
    letter-spacing: 0.02857em;
    color: #1976d2;
    text-transform: uppercase;
  }
`;
